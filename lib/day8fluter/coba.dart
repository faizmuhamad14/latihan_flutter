import 'package:flutter/material.dart';

class LatihanRemake extends StatelessWidget {
  const LatihanRemake({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: Scaffold(backgroundColor: Colors.blue[100]));
  }
}
